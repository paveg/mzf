///|
/// Stub main for native target (chat example requires JS for Claude API)
fn main {
  println("Error: Chat example requires JS target for Claude API support.")
  println("Run with: moon run examples/chat --target js")
}
